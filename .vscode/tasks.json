{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build (Debug)",
      "type": "shell",
      "osx": {
        "command": "mkdir -p ${workspaceFolder}/build && cd ${workspaceFolder}/build && cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build ."
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
